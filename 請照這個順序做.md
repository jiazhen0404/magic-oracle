# 第二階段 · 把付費內容接上

做完這一包，整條線就通了：

**抽籤 → 結果頁出現「完整解籤 NT$99」→ 結帳 → 綠界付款 → 自動解鎖 → 讀到 1300 字的完整解讀**

只剩「寄 PDF 到信箱」還沒做，那是第三階段。

---

## 這包裡有什麼

| 檔案 | 動作 | 說明 |
|---|---|---|
| `src/index.js` | 覆蓋 | 後端加了解鎖與讀取內容兩個功能 |
| `src/extended-love.json` | **新增** | **170 支延伸籤全文（付費內容）** |
| `index.html` | 覆蓋 | 結果頁的「即將開放」換成真的購買按鈕 |
| `extended/reading/index.html` | 新增 | 付費後閱讀完整解籤的頁面（含開封動畫） |
| `checkout/index.html` | 覆蓋 | 之前修好的版本 |
| `checkout/done/index.html` | 覆蓋 | 付款成功後自動解鎖 |
| `privacy/index.html` | 覆蓋 | 條款頁（如果你已經傳過，覆蓋也沒差） |
| `sitemap.xml`、`.assetsignore` | 覆蓋 | |

**這包裡沒有 `wrangler.jsonc`**，因為裡面有你填的 KV 編號，我不能蓋掉。

---

## 付費內容為什麼放在 src 資料夾

`src/extended-love.json` 是你花最多力氣寫的東西，**絕對不能被人直接下載**。

它放在 `src/` 底下，而 `.assetsignore` 裡有一行 `src/`，
意思是：這個資料夾會被打包進後端程式裡執行，但**不會**變成網站上的檔案。

所以沒有人能打開 `unfinished.tw/src/extended-love.json` 把 170 支籤全部載走。
只有付過款、拿到解鎖憑證的人，才能透過後端一次讀一支。

**請不要把這個檔案移到別的資料夾。**

---

## 怎麼做

1. 解壓縮
2. GitHub → **Add file** → **Upload files**
3. 把解壓縮後的**內容**全部拖進去
   （`src`、`extended`、`checkout`、`privacy` 四個資料夾，
   加上 `index.html`、`sitemap.xml`、`.assetsignore`）
4. 說明填「延伸籤上線」→ **Commit changes**
5. 等 Cloudflare 建置完，1～2 分鐘

`.assetsignore` 是隱藏檔，Mac 要先按 `Cmd + Shift + .` 才看得到。

---

## 驗收

### 一、確認內容有載進去

打開 `https://unfinished.tw/api/health`

這次會多一個欄位：

```
"extendedCount":170
```

**必須是 170。** 如果是 0 或沒有這個欄位，代表 `src/extended-love.json` 沒傳到。

### 二、你之前那筆訂單可以直接解鎖

你剛剛測試付款的訂單編號是 `UF260829220222KHXA`，還在暫存裡（24 小時內有效）。

打開：

```
https://unfinished.tw/checkout/done/?no=UF260829220222KHXA
```

畫面會變成「付款成功 · 已經替你解鎖」，下面多一顆 **閱讀完整解籤** 的按鈕。
按下去就會看到「月影未央」的完整解讀，十個段落、兩千多字。

**這一步能過，就代表整條線通了。**

### 三、開封動畫

第一次進入閱讀頁時，會播一段開封動畫：
一封蠟封的信浮現 → 封蠟裂開、金光滲出 → 光暈擴散 → 解讀浮現。約 2.7 秒。

- 點畫面任一處可以跳過
- **同一支籤在同一個分頁只會播一次**，重讀不會再打擾你
- 系統開啟「減少動態效果」的人會自動跳過

用的是你原本就有的 `assets/magic-letter-guide.webp`，沒有新素材。

### 四、走一次完整的真實流程

1. 回首頁 `unfinished.tw`
2. 抽籤 → 選「愛情」→ 走完流程
3. 解籤結果頁往下捲，原本寫「COMING SOON · 即將開放」的地方，
   現在應該是 **「完整解籤 · NT$ 99」**，按鈕寫「閱讀完整解籤　NT$ 99」
4. 按下去 → 結帳頁 → 填信箱、勾兩個同意 → 前往付款
5. 綠界測試頁按右邊橘色的「測試付款請點此」
6. 跳回來自動解鎖 → 讀完整解籤

### 五、確認鎖是真的鎖

開一個**無痕視窗**，直接打：

```
https://unfinished.tw/extended/reading/?id=love_breakup_001
```

應該顯示「**尚未解鎖**」。

**這一項很重要**——如果無痕視窗也能讀到內容，代表鎖沒鎖上，先告訴我不要繼續。

---

## 我測過什麼

在交給你之前，我在本地跑了 27 項自動測試，全部通過，包括：

- 建立訂單的檢查碼格式、金額、回調網址
- **沒勾同意就下單會被擋下**
- 綠界付款通知驗章成功
- **偽造的付款通知會被拒絕（回 400）**
- 訂單狀態查詢**不會外洩信箱與問題**
- 同一筆訂單重複索取憑證，拿到的是同一張
- **沒有憑證讀不到內容（402）**
- **偽造憑證讀不到內容（402）**
- **拿 A 籤的憑證去開 B 籤，讀不到（402）**
- 未付款的訂單不能解鎖
- 170 支籤的編號，結果頁按鈕產生的編號 100% 對得上

另外綠界的檢查碼演算法，我用綠界官方文件的範例參數驗算過，結果完全相同。

---

## 還沒做的事

**1. 寄 PDF 到信箱（第三階段）**

結帳頁上寫了「解籤 PDF 會寄到這裡」，但目前**還不會寄**。
在做完之前，這句話是沒有兌現的承諾。

**所以現在還不能切到正式收款。** 要嘛先做完 PDF，要嘛先把那句話改掉。

**2. 有一件事要你決定**

我看了你的抽籤流程，「想一件事」那一頁的輸入框是**選填**的，
很多人不會打字就直接按「我準備好了」。

原本的設計是「PDF 上會印使用者的問題」，但如果多數人沒填，那一塊會是空的。

三個選擇：
- **照原樣**：有填就印，沒填就不印那一塊
- **改成必填**：拿得到問題，但會擋掉一部分懶得打字的人
- **PDF 不放問題**：只放籤文與解讀，最單純

我建議第一個。這個決定會影響 PDF 的版面，所以要先定。

**3. 切正式收款前要做的事**

- `wrangler.jsonc` 裡的 `"ECPAY_MODE": "stage"` 改成 `"production"`
- Cloudflare 的三個密鑰換成你自己綠界後台的正式資料（你自己貼，不用經過我）
- 找律師看過條款
